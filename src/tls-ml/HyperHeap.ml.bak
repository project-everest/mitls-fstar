type rid = int

type 'a rref = 'a ref

let sel (x:'a ref) = x
    
let upd (x:'a ref) (v:'a) = (x := v)
    
let ralloc i a = ref a

let op_Colon_Equals r v = r:=v

let op_Bang r = !r

let recall r = ()

let new_region = fun x -> 0

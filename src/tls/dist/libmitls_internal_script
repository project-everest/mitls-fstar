# This version script is used on CI because the test needs access to internal
# structures and helpers.
LIBMITLS_0.1 {
    global:
      *;
    local:
      kremlinit_globals;
      __eq__*;
      __neq__*;
      FStar_Bytes_*;
      Prims_*;
};
